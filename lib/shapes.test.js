const {Shapes} = require('./lib/shapes.js');

describe('Shapes', () => {
    describe('run', () => {
        it('should ask four questions and save the user response to create a new "logo.svg" file for the user', () => {

        })
    })
})

describe('Shapes', () => {
    describe('setTextColor', () => {
        it('should set the text color that the user selected', () => {

        })
    })
})

describe('Shapes', () => {
    describe('setShapeColor', () => {
        it('should set the shape color that the user selected', () => {

        })
    })
})

describe('Shapes', () => {
    describe('setText', () => {
        it('should set the text that the user selected', () => {

        })
    })
})
